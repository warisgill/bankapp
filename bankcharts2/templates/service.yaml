apiVersion: v1
kind: Service
metadata:
  name: {{ include "bankcharts2.fullname" . }}
  labels:
    {{- include "bankcharts2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bankcharts2.selectorLabels" . | nindent 4 }}
